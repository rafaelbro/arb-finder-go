{
  "name": "quote-prices",
  "version": "1.0.0",
  "description": "Check if it is should perform swap",
  "author": "Gustavo Kath",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "yarn build && node dist/index.js",
    "build": "rm -rf ./dist && tsc"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "http-status-codes": "^2.1.4",
    "js-big-decimal": "^1.3.4",
    "web3": "^1.3.6"
  },
  "devDependencies": {
    "@types/node": "^15.12.1",
    "typescript": "^4.3.2"
  }
}
